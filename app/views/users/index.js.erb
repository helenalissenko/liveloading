<% if @users.all.count>0 -%>
$('users').append('<%= escape_javascript(render(partial: 'users')) %>');
$('a.hook').attr('href', '<%= users_path(page: (params[:page].to_i || 1)+1, per_page: params[:per_page] || 8) %>').show();
<% else -%>
$('a.hook').hide();
<% end -%>